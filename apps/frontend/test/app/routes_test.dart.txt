// import 'package:flutter/material.dart';
// import 'package:flutter_test/flutter_test.dart';
// import 'package:organisation_app/app/app.dart';
// import 'package:organisation_app/app/routes.dart';
//
// void main() {
//   testWidgets('Test Navigation Routes', (WidgetTester tester) async {
//     // Build our app and trigger a frame.
//     await tester.pumpWidget(const OrganisationApp());
//
//     // Navigate to each route and verify it
//     await _navigateAndVerify(tester, '/');
//     await _navigateAndVerify(tester, '/home');
//     await _navigateAndVerify(tester, '/login');
//     await _navigateAndVerify(tester, '/courses');
//     await _navigateAndVerify(tester, '/todos');
//     await _navigateAndVerify(tester, '/moodle');
//     await _navigateAndVerify(tester, '/chatgpt');
//     await _navigateAndVerify(tester, '/primuss');
//   });
// }
//
// Future<void> _navigateAndVerify(WidgetTester tester, String route) async {
//   // Navigate to the route
//   await tester.pumpWidget(MaterialApp(
//     home: Container(), // Use any widget as the home widget
//     initialRoute: route,
//     routes: appRoutes,
//   ));
//
//   // Wait for animations to complete
//   await tester.pumpAndSettle();
//
//   // Verify that the correct route is pushed
//   expect(find.byKey(ValueKey(route)), findsOneWidget);
// }
